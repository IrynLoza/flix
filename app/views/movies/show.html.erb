<section class="movie-details">
        <div class="details">
            <h2><%= @movie.title %></h2>
            <h3> <%= release_date(@movie) %> (<%= @movie.rating %>) </h3>
            <span>
                <%= total_gross(@movie) %>
            </span>
            <p>
                <%= @movie.description %>
            </p>
        </div>
    </section>